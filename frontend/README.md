# MZ Perfumse Frontend

Frontend-only React application for the MZ Perfumse landing page.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run build
```

Copy `.env.example` to `.env` when the backend API is available, then update `VITE_API_BASE_URL`.
